import { grpc } from "@improbable-eng/grpc-web";
import { CreateUserResponse, UsersResponse } from "@textile/grpc-powergate-client/dist/powergate/admin/v1/admin_pb";
import { Config } from "../../types";
export interface AdminUsers {
    /**
     * Create a new user.
     * @returns Information about the new user.
     */
    create: () => Promise<CreateUserResponse.AsObject>;
    /**
     * List all users.
     * @returns A list of all users.
     */
    list: () => Promise<UsersResponse.AsObject>;
}
/**
 * @ignore
 */
export declare const createUsers: (config: Config, getMeta: () => grpc.Metadata) => AdminUsers;
